# react-fastapi-movie-recommender
From the main project folder, run the FastAPI backend:

``` 
source backend/venv/bin/activate
python3 main.py
```

To run the frontend (node.js, react and axios installed):
```
npm run dev
```
